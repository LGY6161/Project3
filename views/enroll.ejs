<!DOCTYPE html>
<html>
<head>
  <title>수강신청 프로그램</title>
</head>
<body>
  <h1>수강신청 프로그램</h1>
  <h2>수강과목</h2>
  <ul>
    <% for (let i = 0; i < courses.length; i++) { %>
      <li>
        <%= courses[i].name %> (<%= courses[i].time %>)
        <form action="/enroll" method="post">
          <input type="hidden" name="courseId" value="<%= courses[i].id %>">
          <button type="submit">수강신청</button>
        </form>
      </li>
    <% } %>
  </ul>

  <h2>수강신청 목록</h2>
  <ul>
    <% for (let i = 0; i < enrolledCourses.length; i++) { %>
      <li>
        <%= enrolledCourses[i].name %> (<%= enrolledCourses[i].time %>)
      </li>
    <% } %>
  </ul>
</body>
</html>
